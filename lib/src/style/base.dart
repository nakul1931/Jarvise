import 'package:flutter/cupertino.dart';

class BaseStyles {
  static EdgeInsets get topPadding => EdgeInsets.only(top: 25.0);
  static EdgeInsets get containerPadding => EdgeInsets.all(10.0);
  static EdgeInsets get loginContainerPadding => EdgeInsets.all(25.0);
}
